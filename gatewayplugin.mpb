project {
  after += LibGatewayConnector LibJSON

  includes += $(GATEWAY_ROOT)/LibGatewayConnector $(GATEWAY_ROOT)/LibJSON
  libs += gatewayconnector json
  libpaths += $(GATEWAY_ROOT)/build/lib
  
  specific(prop:windows) {
    macros += JSON_DLL_BUILD
  }
}
